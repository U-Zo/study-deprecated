

















CREATE SEQUENCE EMP_SEQ
INCREMENT BY 1
START WITH 100
MAXVALUE 9999
NOCACHE
NOCYCLE;

SELECT * FROM USER_SEQUENCES;

INSERT INTO EMP
VALUES (EMP_SEQ.NEXTVAL, 'WOOJO', '인사', NULL,
    SYSDATE, 2500, 300, 40);

SELECT * FROM EMP;

SELECT EMP_SEQ.CURRVAL FROM DUAL;
ALTER SEQUENCE EMP_SEQ
INCREMENT BY 1;

SELECT EMP_SEQ.NEXTVAL FROM DUAL;

ALTER SEQUENCE EMP_SEQ
INCREMENT BY 2
MAXVALUE 10000
NOCACHE
NOCYCLE;

SELECT EMP_SEQ.CURRVAL FROM DUAL;
INSERT INTO EMP
VALUES (EMP_SEQ.NEXTVAL, 'WOOJO', '인사', NULL,
    SYSDATE, 2500, 300, 40);
SELECT * FROM EMP;

DROP SEQUENCE EMP_SEQ;

CREATE SEQUENCE DEPT_DEPTNO_SEQ
    START WITH 10
    INCREMENT BY 10
    MAXVALUE 100
    MINVALUE 5
    CYCLE
    NOCACHE;

SELECT DEPT_DEPTNO_SEQ.NEXTVAL, DEPT_DEPTNO_SEQ.CURRVAL FROM DUAL;
DESC DEPT;

INSERT INTO DEPT
VALUES (DEPT_DEPTNO_SEQ.NEXTVAL, '우조', '서울');

SELECT * FROM EMP;

SELECT * FROM TB_DEPARTMENT
WHERE DEPARTMENT_NAME = '체육학과';
SELECT * FROM TB_DEPARTMENT
WHERE DEPARTMENT_NAME = '행정학과';

SELECT * FROM EMP
WHERE ENAME = 'SMITH';

CREATE INDEX EMP_ENAME_IDX
ON EMP(ENAME);

SELECT * FROM EMP
WHERE ENAME = 'SMITH';

-- USER01 에게 DEPT 테이블의 SELECT 권한 부여
GRANT SELECT
ON DEPT
TO USER01;
-- 특정 컬럼의 UPDATE 권한 부여
GRANT UPDATE(DNAME)
ON DEPT
TO USER01;